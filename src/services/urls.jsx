import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const urls = () => {
  return (
    <View>
      <Text>urls</Text>
    </View>
  );
};

export default urls;

const styles = StyleSheet.create({});
